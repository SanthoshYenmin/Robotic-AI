import AboutCinematic from "@/components/sections/AboutCinematic";

export default function AboutPage() {
  return (
    <div className="w-full">
      {/* We can add a simple spacer here if needed, or let AboutCinematic take the full height immediately */}
      <AboutCinematic />
    </div>
  );
}
